/**
public class Notes {

    // in main
    Word w = new Word(input);

    // in word

    save new word that user puts in
    guesses get set to 0, increment by 1
    array list of letters (starts empty)
        Match receives input, matching letters to the other word


    }
}

        import java.awt.*;

public class Turkey {
    private String name;

    private Color color;

    private int Height;

    public Turkey(String s) {
        this.name = s;
        color = Color.pink;
        Height = 300;
    }

    //Getter method for Color
    public Color getColor() {
        return this.color;
    }

    public void talk() {
        System.out.println(this.name + ": Gobble Gobble!");
    }

    public String getName() {
        return this.name;
    }

    public void setColor(Color c) {
        this.color = c;
    }


    // toString method
    public String toString() {
        String output;
        output = "[ name =" + this.name +
                " color = " + this.color +
                " age =" + this.Height +
                "]";
        return output;
    }
}
**/
